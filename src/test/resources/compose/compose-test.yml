version: '2'
services:
  mongo-test:
    image: mongo:3.4
    container_name: mongo-test
    ports:
      - "27017:27017"

  mock-server-test:
    image: jamesdbloom/mockserver:mockserver-5.2.3
    container_name: mock-server-test
    ports:
      - "1080:1080"
